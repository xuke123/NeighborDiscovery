#ifndef WAKEUP_TIME_H
#define WAKEUP_TIME_H
#include <stdint.h>

enum {
    INVALID_NODE = UINT8_MAX,
    INVALID_INDEX = UINT8_MAX,
    INVALID_OFFSET = INT32_MAX,
    BIAS = 2,
};
#endif 
